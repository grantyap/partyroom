{
  "name": "@partyroom/lyrics-worker",
  "private": true,
  "scripts": {
    "check-types": "python3 -m compileall -q app",
    "dev:mac": "bun --env-file=../../.env scripts/dev-macos.ts",
    "dev:mac:build": "bun --env-file=../../.env scripts/dev-macos.ts",
    "dev:mac:host": "bun --env-file=../../.env scripts/dev-macos.ts",
    "dev:mac:host:build": "bun --env-file=../../.env scripts/dev-macos.ts",
    "lock": "uv lock",
    "sync": "uv sync --locked --extra cpu",
    "test": "docker build -t partyroom-lyrics-worker -f Dockerfile ../.. && docker run --rm -v \"$PWD:/src:ro\" -e PYTHONPATH=/src partyroom-lyrics-worker python3 -m unittest discover -s /src/tests"
  }
}
