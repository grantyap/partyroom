{
  "name": "@partyroom/annotations-worker",
  "private": true,
  "scripts": {
    "check-types": "python3 -m compileall -q app",
    "lock": "uv lock",
    "sync": "uv sync --locked",
    "test": "docker build -t partyroom-annotations-worker -f Dockerfile ../.. && docker run --rm -v \"$PWD:/src:ro\" -e PYTHONPATH=/src partyroom-annotations-worker python3 -m unittest discover -s /src/tests"
  }
}
